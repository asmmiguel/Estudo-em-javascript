const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const subtracao = d -b
const mult = a * b
const divisao = b / a
const modulo = d % 2

console.log(soma, subtracao, mult, divisao, modulo)